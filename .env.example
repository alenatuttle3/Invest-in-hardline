# Slack incoming webhook that receives investor fit-check submissions.
# Create one at https://api.slack.com/apps → Incoming Webhooks → Add New Webhook,
# pick the channel, and paste the URL here (and in your hosting provider's env vars).
SLACK_WEBHOOK_URL=
